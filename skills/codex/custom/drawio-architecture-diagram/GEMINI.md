Use this directory to generate, refine, audit, and export draw.io architecture diagrams.

Read `SKILL.md`, then choose a template from `templates/diagrams/architecture/`. Do not create separate SVG/PNG drawings when `.drawio` can be exported by draw.io.

