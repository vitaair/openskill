Use this directory for professional draw.io architecture diagram generation, editing, visual review, and `.drawio` to SVG/PNG export consistency.

Start with `SKILL.md`. Use `templates/diagrams/architecture/` for reusable layouts. Use `.drawio` as the only formal source and export SVG/PNG through draw.io.

