# Claude Project Memory

This project uses the shared AI Memory Palace method:

`<LOCAL_SKILL_WORKSPACE>/记忆宫殿SKILL`

For long-term memory, handoffs, reusable notes, and Agent-readable indexes, read `<LOCAL_SKILL_WORKSPACE>/skill-index.md`, then the shared `SKILL.md` and `ai-memory-index.md`, then update this project's memory file using the shared `ai-memory-template.md`.

Do not copy the full method into this project.
