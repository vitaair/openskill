# json-render Agent Instructions

Use this directory when designing, implementing, reviewing, or explaining json-render style generative UI.

Read order:

1. `SKILL.md`
2. `json-render-index.md`

Core model: define a catalog of allowed components/actions, let AI generate only JSON specs inside that catalog, then render through a registry with safe state/action/validation handling.

