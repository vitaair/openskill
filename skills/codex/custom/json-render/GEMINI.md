# json-render For Gemini

When working on json-render style generative UI, use this folder.

Read `SKILL.md`, then `json-render-index.md`.

The capability is catalog-constrained AI UI generation: AI emits JSON specs, the host validates and renders them through known components and safe actions.

